<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- network (only once) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Storage access: keep READ_EXTERNAL_STORAGE for older Android versions.
         For Android 13+ you may request READ_MEDIA_* at runtime when listing images/videos. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- WRITE_EXTERNAL_STORAGE is deprecated on newer Androids; avoid unless absolutely needed -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <!-- Optional: for Camera capture -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Optional: media location on some devices -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- Location / network perms kept only if your app requires them -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
